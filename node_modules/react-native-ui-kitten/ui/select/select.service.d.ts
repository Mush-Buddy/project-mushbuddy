import { KeyExtractorType } from './select.component';
import { SelectOptionType } from './selectOption.component';
declare type Options = SelectOptionType | SelectOptionType[];
interface SelectServiceOptions {
    multiSelect?: boolean;
    keyExtractor?: (option: SelectOptionType) => string;
}
export declare class SelectService {
    private strategy;
    constructor(options?: SelectServiceOptions);
    select: (option: SelectOptionType, selectedOptions: Options) => Options;
    isSelected: (option: SelectOptionType, options: Options) => boolean;
    toStringOptions: (options: Options) => string;
    static isGroup: (option: SelectOptionType) => boolean;
    static toStringOption: (option: SelectOptionType) => string;
    static isEqualOptions: (lhs: SelectOptionType, rhs: SelectOptionType, keyExtractor?: KeyExtractorType) => boolean;
}
export {};
